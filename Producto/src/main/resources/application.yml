server:
  port: 8080

spring:
  application:
    name: productoService

  data:
    mongodb:
      host: mongo # <-- Asegúrate de que coincide con el nombre del contenedor
      port: 27017
      database: productoservice_db
